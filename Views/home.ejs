<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Anime Talks</title>
    <style>
        body { font-family: Arial; padding: 20px; }
        .card { border: 1px solid #ccc; padding: 10px; margin: 15px 0; border-radius: 5px; }
        .card img { width: 300px; border-radius: 5px; }

        .delete-button {
    margin-top: 15px;
    padding: 0.8rem 1.5rem;
    background-color: #dc3545;
    color: white;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    cursor: pointer;
    transition: background 0.3s ease;
}
.delete-button:hover {
    background-color: #a71d2a;
}

    </style>
</head>
<body>
    <h1> Anime Talks </h1>
    <% for(post of data){ %>
    <div class="card">
        <h2><%= post.username %></h2>
        <img src="<%= post.image %>" alt="Image">
        <p><%= post.description %></p>
        <a href="/home/<%= post.id %>"> In Detail </a>&nbsp;&nbsp;
        <a href="/home/<%= post.id %>/edit"> Edit  </a>
        <form action="/home/<%= post.id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?');">
            <button type="submit" class="delete-button">Delete Post</button>
        </form>
    </div>
    <% }  %>
    <a href="/home/create"> New Post  </a>
    
</body>
</html>